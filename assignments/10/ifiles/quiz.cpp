float LinearTransform(float x, float C, float D, float E, float F)
{
   float A = 0.0f; // TODO: Find A in terms of C, D, E, F
   float B = 0.0f; // TODO: Find B in terms of C, D, E, F (Not A!)
   return A * x + B;
}
